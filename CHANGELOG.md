# 📝 CHANGELOG — WhatsApp Bot 1.18

Versi saat ini: **1.18** | Dicatat: **11-09-2026**

---

## 🎮 Daftar Fitur Bot (per 11-09-2026)

### 💰 Money & Ekonomi
- **Profil & Saldo** — `!me` — profil, saldo, foto profil, nomor WA terformat, nama berupa tag (ditambahkan pada: 02-08-2026)
- **Klaim Harian** — `!claim` — reward gratis harian (ditambahkan pada: 02-08-2026)
- **Transfer Money** — `!transfer @tag [jumlah|all]` / `!tf` — kirim Money ke pemain lain, tanpa cooldown/pajak (ditambahkan pada: 02-08-2026)
- **Ganti Nama** — `!gantinama [nama]` / `!rename` — ganti nama di semua game (100k) (ditambahkan pada: 02-08-2026)
- **Bank & Bunga** — `!bank` / `!depo` / `!tarik` / `!klaimbunga` — simpan uang di bank, bunga 1% per 24 jam (ditambahkan pada: 07-09-2026)
- **Begal** — `!begal @user` / `!rob` — rampok cash lawan, peluang sukses 50% (ditambahkan pada: 07-09-2026)
- **Bobol** — `!bobol @user` — bobol tabungan bank lawan, peluang 25% hasil besar (ditambahkan pada: 08-09-2026)
- **Top Terkaya** — `!top money` — peringkat pemain terkaya (ditambahkan pada: 02-08-2026)
- **Top Bust Blackjack** — `!top bust` — top 5 kerugian Blackjack per grup (ditambahkan pada: 17-08-2026)
- **Top Begal/Bobol** — `!top begal`, `!top bobol` — leaderboard jarahan begal & bank (ditambahkan pada: 08-09-2026)
- **Gacha** — `!gacha [1/10]` — hadiah uang & item langka (ditambahkan pada: 15-08-2026)

### 🎮 Game Utama
- **FishIt Mancing** — `!fishit` — daftar, lokasi, mancing, inv, jual, fishdex, profil, claim, top player, gantinama, shop joran & umpan (ditambahkan pada: 02-08-2026)
- **Pokemon** — `!p` — daftar, spawn, serang, tangkap, kabur, koleksi, tim, toko, profil, rank, battle, evolusi, claim (ditambahkan pada: 02-08-2026)
- **RPG Petualang** — `!rpg` — profil, claim, tebang, tambang, fight, shop, craft, equip, kelas & skill, raid boss, tempa, duel, top (ditambahkan pada: 02-08-2026)
- **Casino** — `!casino` — slot payout 2x/3x/5x + judi material RPG & ikan FishIt (ditambahkan pada: 02-08-2026)
- **Blackjack** — `!bj @tag` / `!blackjack` — tantang, terima, hit, stand; tiap pemain 2 kartu & natural blackjack (ditambahkan pada: 02-08-2026)
- **Togel** — `!togel [angka] [bet]` — pasang nomor 2D/3D/4D (ditambahkan pada: sebelum 02-08-2026 — tidak tercatat)
- **Mahjong** — `!mahjong` — solo & multiplayer (ditambahkan pada: sebelum 02-08-2026 — tidak tercatat)

### 🕹️ Mini Game
- **Buckshot Roulette** — `!br` / `!buckshot` — duel shotgun vs bot (solo) atau PvP 2-4 pemain (ditambahkan pada: 07-09-2026)
- **Tebak Boom** — `!tebakboom [bet]` — taruhan tebak angka beracun (ditambahkan pada: 20-08-2026)
- **Tebak Angka** — `!tebak` — tebak 1-100 dapat hadiah (ditambahkan pada: 26-08-2026)
- **Family 100** — `!family100` — kuis asah otak (ditambahkan pada: 02-08-2026)
- **Batu-Gunting-Kertas** — `!suit @tag` (ditambahkan pada: 02-08-2026)
- **Tic Tac Toe** — `!ttt @tag` (jawab 1-9) (ditambahkan pada: 02-08-2026)
- **Ular Tangga 2D** — `!snakes` (`roll` / `lempar`) (ditambahkan pada: 02-08-2026)
- **Pet** — `!pet` — adopsi pet acak, rawat (makan ikan FishIt), evolusi 4 stage, battle PvP (ditambahkan pada: 12-08-2026)

### 🎉 Fun & Sosial
- **Cari Jodoh** — `!jadian` — jodoh random di grup (ditambahkan pada: 26-08-2026)
- **Jokes & Kutipan** — `!jokes` / `!lucu`, `!quote` / `!kutipan` (ditambahkan pada: 26-08-2026)
- **Truth or Dare** — `!truth` / `!jujur`, `!dare` / `!tantangan` (ditambahkan pada: 26-08-2026)
- **Bola Ramalan** — `!8ball [tanya]` / `!magic` (ditambahkan pada: 26-08-2026)
- **Lempar Dadu** — `!dice [n]` / `!roll` (ditambahkan pada: 26-08-2026)
- **Base64** — `!base64 [enc/dec]` / `!b64` (ditambahkan pada: 26-08-2026)
- **Kalkulator** — `!calc [rumus]` / `!kalkulator` / `!math` (ditambahkan pada: 26-08-2026)

### 🎵 Media & Downloader
- **YouTube** — `!play [judul]` — download audio/video (balas `!audio` / `!video`) (ditambahkan pada: 02-08-2026)
- **Lirik** — `!playlirik [judul]` — lagu + lirik + card player interaktif (ditambahkan pada: 06-09-2026)
- **TikTok** — `!tt [link]` — download video no-wm (`!ttaudio` / `!ttvideo`) (ditambahkan pada: 02-08-2026)
- **Kirim Ulang Media** — `!send` (ditambahkan pada: 02-08-2026)
- **Stiker** — `!s` / `!sticker` — ubah media jadi stiker (ditambahkan pada: 02-08-2026)
- **Konversi** — `!toimg`, `!tovid`, `!toaudio`, `!change [rasio]` (ditambahkan pada: 02-08-2026)
- **Stiker Meme** — `!smeme [teks]` (ditambahkan pada: 02-08-2026)
- **Foto Profil** — `!getpp` — ambil foto profil WA (ditambahkan pada: 02-08-2026)

### ⚙️ Perintah Admin (owner juga bisa)
- **Ban** — `!ban @user [durasi]` / `!unban` / `!banlist` — ban member di grup tersebut (di owner berlaku global ke semua grup), bisa berdurasi (`10s`/`5m`/`2h`/`1d`/`1w`/`1mo`) (ditambahkan pada: 02-08-2026)
- **Hidetag** — `!hidetag [teks]` — tag semua member tanpa menampilkan @ (ditambahkan pada: 12-08-2026)
- **Hapus Pesan Bot** — `!del` / `!delete` — hapus pesan bot/member yang di-reply (ditambahkan pada: 02-08-2026)
- **Status WA** — `!sw` — pasang status di dalam grup (Group Status) (ditambahkan pada: 29-08-2026)

### ⚙️ Perintah Owner (semua perintah admin di atas + berikut)
- **Allow Group** — `!allowgroup` — batasi/pulihkan akses grup, preset & durasi bebas (ditambahkan pada: 02-08-2026)
- **PM Mode** — `!mode on/off` — toggle layanan PM orang asing (ditambahkan pada: 07-09-2026)
- **Izin GetPP** — `!acc` / `!unacc` — beri/cabut izin `!getpp` (ditambahkan pada: 02-08-2026)
- **Reset Akun** — `!reset [rpg|fishit|pokemon|money]` — reset akun game atau saldo (ditambahkan pada: sebelum 02-08-2026 — tidak tercatat)
- **Bansos** — `!bansos @user [jumlah]` — beri Money gratis, tidak mengurangi saldo owner (ditambahkan pada: sebelum 02-08-2026 — tidak tercatat)
- **AI Asisten** — `!bot` + free-text owner berakhiran `.` — DeepSeek via proxy, ingatan fakta & riwayat, jawab pesan reply, pencarian internet eksplisit (ditambahkan pada: 16-08-2026; web search: 10-09-2026)

### 📋 Umum & Dev
- **Menu & AFK** — `!menu` / `!game`, `!afk [alasan]`; dashboard NEXA PLAYGROUND dan quick-start navigasi (ditambahkan pada: 02-08-2026; penyegaran: 10-09-2026)
- **Test Suite** — `npm test` — 154 test untuk semua logika bot (db, ekonomi, bank/begal/bobol, battle, fishing, pokemon, rpg, casino, minigame, buckshot, router); berjalan di DB/state temp (anti menyentuh data asli) (ditambahkan pada: 02-08-2026)

---

## ✍️ Riwayat Perubahan

- **02-08-2026 13:40 WIB** — Versi 1.1: dokumentasi daftar fitur (dibuat)
- **02-08-2026** — Versi 1.2: upgrade `!me` — kini menampilkan foto profil (fallback teks jika PP tidak tersedia), nomor WA terformat (+62), dan nama berupa tag @mention.
- **02-08-2026** — Versi 1.3: penambahan **test suite otomatis** (`test/`):
  - `data/db.js` — path DB & backup bisa di-override env `BOT_DB_PATH` / `BOT_BACKUP_DIR`
  - `handlers/utilityCommands.js` — path state AFK bisa di-override env `BOT_AFK_STATE_PATH`
  - `handlers/minigameCommands.js` — path state Family 100 bisa di-override env `BOT_FAMILY_STATE_PATH`
  - `package.json` — script `npm test` → `node test/run.js`
  - 92 test lulus (9 suite), menjalankan handler & engine asli dengan mock socket
  - Known issue yang kini terdokumentasi & di-regression-test: C1a/C1b (reset+reregister cetak money), C2 (catch farm pokemon), C3 (loop apel RPG), C4 (cooldown mancing 0), H1 (battle tanpa batas ronde), H3 (selisih harga jual ikan engine vs handler), H4 (jual substring ikan), H6 (duel tanpa konfirmasi nominal), + family100 return value
- **02-08-2026** — Versi 1.4: penambahan **Transfer Money universal** (`!transfer` / `!tf`):
  - Baru: `handlers/transferCommands.js` — kirim Money ke pemain lain via tag atau reply
  - Command: `!transfer @user <jumlah|all>` dan alias `!tf @user <jumlah|all>`
  - Bisa dipakai semua user (tanpa wajib daftar game), pakai Money global
  - Tanpa cooldown, tanpa minimal transfer, tanpa pajak, support `all` = kirim semua saldo
  - Hapus: `!fishit gift` (transfer khusus FishIt, diganti oleh fitur universal)
  - Update: `handlers/fishingCommands.js`, `handlers/gameRouter.js`, `handlers/router.js`, `handlers/menuCommands.js`
- **07-08-2026** — penambahan **Judi Material RPG & Ikan FishIt di Casino**:
  - `!casino material [id] [jumlah|all]` — taruhan semua material RPG (wood, stone, iron, gold_ingot, diamond, adamantium, uru); tanpa arg = daftar material milik
  - `!casino fish [nama ikan] [jumlah|all]` — taruhan semua ikan di inventory FishIt (nama multi-kata didukung); tanpa arg = daftar ikan milik
  - `!casino help` — panduan singkat; menu utama di-update
  - Model payout: menang = taruhan item dikembalikan ×2/×3/×5, kalah = item hangus (peluang sama dengan casino money)
  - Nilai ikan memakai rumus harga `!fishit jual`; map harga dipindah ke `game/fishingEngine.js` (export `FISH_SELL_TIERS`) agar satu sumber
  - Update: `handlers/casinoCommands.js`, `game/fishingEngine.js`, `handlers/fishingCommands.js`, `handlers/menuCommands.js`
- **07-08-2026** — penyeimbangan **ekonomi (harga jual/beli & claim)**:
  - `data/rpgData.js` — material RPG kini punya `sellPrice` terpisah (jual = 60% beli): wood 6, stone 12, iron 60, gold_ingot 350, diamond 1200, adamantium 3000, uru 9000; `price` tetap = harga beli
  - `handlers/rpgCommands.js` — `!rpg jual` (list/all/single) memakai `sellPrice`; shop & beli tetap `price`
  - `handlers/casinoCommands.js` — nilai taruhan & daftar material memakai `sellPrice`
  - `handlers/claimCommands.js` — rebalance ringan (EV ~400/hari): Money 75-200, Umpan Cacing 4-7, Pokeball 2-4, Apel 1-2, Emas 3% (nilai jual dinamis dari `sellPrice`)
  - `game/fishingEngine.js` — harga jual ikan **tidak diubah**; "Est. Harga" saat mancing disamakan ke harga jual nyata (`FISH_SELL_TIERS`, fix selisih H3), map `tierMultipliers` tak terpakai dihapus
- **07-08-2026** — **sinkronisasi nama item RPG ke Bahasa Indonesia** (ID Inggris & alias Indonesia keduanya tetap didukung):
  - `data/rpgData.js` — semua material, item shop, senjata, zirah, & alat punya field `alias` (kayu, batu, besi, emas, berlian, adamantium, uru; apel, ramuan, ramuan+, roti; pedang kayu…pedang uru; zirah batu…zirah uru; cangkul kayu…uru; kapak kayu…uru) + resolver `resolveMaterialId`, `resolveShopItemId`, `resolveEquipmentId`, `resolveCategory`, `CATEGORY_ALIASES`
  - `handlers/rpgCommands.js` — `!rpg shop material/konsumsi` menampilkan `[alias]` & instruksi beli pakai nama; `!rpg beli`, `!rpg jual`, `!rpg tempa`, `!rpg equip` menerima nama Indonesia (multi-kata, mis. *pedang kayu* / *zirah batu*) maupun ID Inggris lama (stone, sword_wood, dst); menu kategori tempa pakai *pedang/zirah/kapak/cangkul*
  - `handlers/casinoCommands.js` — `!casino material` menerima nama Indonesia (mis. *berlian*, *kayu*) dan daftar material menampilkan `[alias]`
  - Data pemain lama (key Inggris) tetap kompatibel — tidak ada migrasi DB
- **07-08-2026** — **penyeimbangan ekonomi FishIt** (harga jual ikan, joran, umpan):
  - `game/fishingEngine.js` — kurva `FISH_SELL_TIERS` di-flatten menjadi "nilai per ekor" landai (~70 × 2.5^(tier-1)); bobot ikan tetap dipakai sebagai pengali (ikan besar lebih mahal). EV/cast per lokasi turun drastis: L1 ~100 → L3 ~555 → L5 ~2.000 → L7 ~25.000 → L9 ~285.000 (sebelumnya L3 ~83rb → L9 triliunan)
  - Reduksi zonk umpan diperkuat: `baitBonus × 0.1` → `× 1.0` dengan floor 5% (lokasi 4+), supaya umpan mahal benar-benar menurunkan zonk (L7: 83% → 43% dgn Esensi)
  - `data/gameData.js` — harga joran disesuaikan agar balik modal ~15-25 mnt: rod_2 15rb, rod_3 22rb, rod_4 83rb, rod_5 175rb, rod_6 300rb, rod_7 500rb, rod_8 1jt, rod_9 1,5jt, rod_10 2,5jt, rod_11 5jt, rod_12 15jt; harga umpan ~5% EV lokasi pemakaian: Udang 30, Ikan Kecil 60, Cumi 100, Luminous 250, Daging 500, Kristal 1rb, Orb 2.500, Esensi 4rb, Serpihan 8rb, Soul 15rb (Cacing 10 & Pelet 50 tetap)
  - `handlers/fishingCommands.js` — **fix bug**: jual ikan satuan (`!fishit jual [ikan] [jml]`) sebelumnya menampilkan "Dapat X Money" tapi TIDAK menambah saldo; kini `addMoney` dipanggil
  - Nilai ikan di casino otomatis ikut turun (dipakai `fishUnitPrice`); `!fishit jual`, Est. Harga, dan casino memakai satu sumber harga
  - Tidak diubah: logika pilih umpan otomatis (terbaik) & cooldown 0
- **10-08-2026** — **standarisasi tampilan dropdown interaktif**:
  - `handlers/menuCommands.js` — `!game` digabung jadi 1 dropdown `🎮 Game` berisi 3 section kategori (FishIt/Pokemon/RPG), sebelumnya 3 dropdown terpisah tanpa kategori; footer dirapikan
  - `handlers/gameRouter.js` — footer menu game diseragamkan: `🎣 FishIt — pilih aksi`, `🎮 Pokemon — pilih aksi`, `⚔️ RPG — pilih aksi` (sebelumnya `Pokemon`/`RPG` tanpa emoji)
  - Tidak ada perubahan ID command — semua id dropdown tetap sama
- **10-08-2026** — **fix fitur `!del` (hapus pesan bot)**:
  - `handlers/adminCommands.js` — root cause: `quotedSender` fallback ke `remoteJid` (JID grup) saat `contextInfo.participant` kosong → salah tolak "hanya pesan bot" & delete key salah.
  - Logika baru: deteksi via `contextInfo.participant` saja; pesan bot (participant kosong/cocok BOT_JID) dihapus `fromMe:true` (selalu berhasil), pesan member dihapus `fromMe:false` (hanya berhasil bila bot admin grup). Hapus pesan `!del` ikut cuma bila bot admin grup.
  - Gagal hapus karena bot bukan admin grup → **diam** (tanpa peringatan). Peringatan "hanya admin bot" tetap hanya untuk member.
  - `.env` — `ADMIN_JID`/`BOT_JID` diubah ke bentuk LID (`@lid`) agar cocok dengan `participant` bentuk LID dari WA; `utils/jid.js` — alias bot LID→PN ditambahkan di `JID_ALIASES`.
- **12-08-2026** — **Rombak struktur handler (refactor besar, logika tidak diubah)**:
  - `handlers/` tak lagi flat — dibagi subfolder: `game/<game>/` (fishit, pokemon, rpg, casino, togel + `minigame/` family100/ttt/snakes/suit), `media/` (download + commands), `economy/` (claim, transfer, profile), `admin/`, `utility/` (utilityCommands, menuCommands).
  - `handlers/gameRouter.js` (5 router + semua pre-handler prompt) dipecah menjadi `handlers/game/<game>/router.js` — tiap game punya router & commands sendiri. `game/togel/router.js` tipis re-export.
  - `handlers/minigameCommands.js` dipecah per game; `handlers/mediaCommands.js` dipecah: infrastruktur download (yt-dlp/ffmpeg/antrian/cooldown) ke `handlers/media/download.js`, handler media tetap di `handlers/media/commands.js`.
  - `data/family100.js` tetap di `data/` (datar soal besar); handler Family 100 pindah ke `handlers/game/minigame/family100.js`.
  - Path relatif file yang berpindah naik satu level (`../data` → `../../data` dst.) — sudah diverifikasi via `npm test` & `node --check` seluruh file.
  - **Test suite**: folder `test/` lama hilang & `npm test` rusak → diganti `test/smoke.js` (15 cek: route tidak throw untuk 15 pesan acak + 15 perintah kunci tiap kategori merespons; berjalan di sandbox DB/state temp). `package.json` test diarahkan ke `node test/smoke.js`.
  - Tidak mengubah logika/validasi/ekonomi/enjin/harga sama sekali.
- **12-08-2026** — **Rombak RPG**: kelas, skill aktif, raid boss, material & equip endgame, balancing:
  - **Kelas** (`data/rpgData.js` `CLASSES`): 6 kelas — Petarung ⚔️ (+ATK), Penambang ⛏️ (+hasil tambang), Pemburu 🏹 (+crit), Penyihir 🔮 (+skill dmg), Penyembuh 💚 (+heal), Perampok 🗡️ (+reward). Pilih gratis pertama (`!rpg kelas`), ganti = 1 Kontrak Kelas 📜 (shop 50rb / drop raid) + money progresif (`!rpg kelas [nama]`). Bonus kelas masuk `game/rpgEngine.js` `getPlayerStats`.
  - **Skill aktif** (`SKILLS`): 2 skill per kelas (damage/heal + cooldown). PvE `!rpg lawan` otomatis pakai skill aktif saat cooldown habis; duel `!rpg serang skill [nama]` manual (cooldown per pemain di state duel). Pilih skill: `!rpg skill`.
  - **Raid grouping** (`handlers/game/rpg/raid.js`, state `db.rpgRaids`): `!rpg raid mulai [boss]` → `!rpg raid join` (2-6 pemain, 1 mnt join) → `!rpg raid serang` / `!rpg raid skill` giliran terkoordinasi (maks 12 ronde). Boss: Naga Kuno 🐲, Phoenix 🦅, Bintang Jatuh 💫. Semua peserta menang dapat material raid + exp + money; kalah = HP 20%. Cooldown grup 2 jam.
  - **Material baru tier 13-15** (hanya dari raid): Jantung Naga 🔥, Bulu Phoenix 🪶, Pecahan Bintang 💫 + equip tier 13-15 (pedang/zirah/kapak/cangkul) dengan resep craft.
  - **Balancing**: kurva monster 1-15 dirapikan (sebelumnya jomplang: M9 HP > M10, M12 < M11) jadi naik monotonic; reward disesuaikan.
  - Update menu, profil (kelas & skill), dropdown, `test/smoke.js` (kelas/skill/raid merespons).
- **12-08-2026** — **Fitur Achievement FishIt**:
  - **Data** (`data/fishitAchievements.js`): daftar achievement deklaratif `FISHIT_ACHIEVEMENTS` — tiap entri `{ id, name, desc, emoji, check(p), progress(p), reward }`. 14 achievement berjenjang: tangkapan (10/100/1000), fishdex (10/20/30/semua 53), tier langka (5+/8+/10+), bobot raksasa (100kg/1.000kg/10.000kg). Reward Money otomatis 5rb → 250rb.
  - **Engine** (`handlers/game/fishit/achievements.js`): `evaluateAchievements(p)` loop semua, unlock yang `check(p)` true & belum dicatat, simpan `p.achievements[id]=timestamp`, auto-credit reward, return daftar baru; `renderUnlocked` & `renderAchievements` untuk notifikasi/list.
  - **Hookup** (`handlers/game/fishit/commands.js`): setelah tangkapan sukses di `cmdMancing`, eval & sisipkan baris `🏆 ACHIEVEMENT UNLOCKED!` di box hasil. State baru `p.achievements = {}` di `data/db.js` (`createFishingPlayer` & `autoRegisterUser`).
  - **Command**: `!fishit achievement` / `!fishit achv` / `!fishit misi` — list semua achievement (✅ selesai + reward, ⭕ progress) di `handlers/game/fishit/router.js`. Menu & dropdown `!fishit menu`, help diperbarui.
  - Reward tidak ganda (timestamp tercatat); tidak mengubah harga/ekonomi lain.
- **12-08-2026** — **Fitur `!hidetag`** (tag semua member, khusus admin):
  - `handlers/admin/adminCommands.js` — handler `handleHidetag`: `!hidetag yo` = kirim teks "yo" + mention semua member; `!hidetag` (reply pesan) = tag semua member + quote pesan yang direply. Sintaks: `body` trigger, gate `sameUser(ADMIN_JID)`, wajib grup, peserta via `sock.groupMetadata(chatId).participants`, bot sendiri tidak di-mention.
  - `handlers/router.js` — `handleHidetag` didaftarkan di `HANDLERS` (dekat handler admin).
  - `CHANGELOG.md` — daftar fitur di "Lainnya" diperbarui.
- **12-08-2026** — **Fitur Pet `!pet`** (rawat + evolusi + PvP):
  - **Data** (`data/db.js`): koleksi baru `petPlayers` (key = JID) `{ jid, ownerName, species, nickname, level, exp, stage, evoReqMet, wins, losses, lastFed, createdAt }` + akses (`createPetPlayer`/`getPetPlayer`/`updatePetPlayer`/`deletePetPlayer`/`getPetLeaderboard`) & migrasi key real-JID.
  - **Data** (`data/petData.js`): `PET_SPECIES` 5 tipe (api/air/dedaunan/kilat/biasa) × 4 stage, `STAGE_MULT=[1,1.35,1.7,2.1]`, `EVO_REQS` — stage1→2: level 10 + ikan tier≥6; stage2→3: level 20 + tier≥10; stage3→4: level 30 + tier≥12.
  - **Engine** (`game/petEngine.js`): `getPetStats` (base × (1+lvl×0.08) × stageMult), `fishExp` (tier×25+berat), `feedFish` (set `evoReqMet` saat tier≥ambang), `applyExp` (level up), `tryEvolve` (level + `evoReqMet` → stage++, syarat ikan dikonsumsi), `computeDamage` (atk−def/2, crit 15% ×1.8). Self-check `node game/petEngine.js`.
  - **Command** (`handlers/pet/commands.js` + `handlers/pet/router.js`): `!pet` (menu), `!pet adopt` (acak, gratis, 1/user), `!pet profil`, `!pet makan <ikan> [jml]` (konsumsi ikan dari inventori FishIt — match & potong sama seperti `!fishit jual`; tanpa arg = snack gratis +5 exp cooldown 30 mnt), `!pet ganti [nama]`, `!pet lepas ya`, `!pet lawan @user` → `!pet terima` → `!pet serang`/`!pet bertahan` (PvP 1v1 per grup, state in-memory transient), `!pet batal`, `!pet top`.
  - **Routing/Menu**: `handlers/router.js` daftarkan `handlePet`; `!menu` & `!game` tambah `!pet menu`; `test/smoke.js` case `!pet`, `!pet adopt`, `!pet top`.
- **13-08-2026** — **RPG: balancing skala ×10, raid gear gate + knock-out, tombol interaktif, cooldown raid dihapus, dropdown `!rpg tempa`**:
  - **Balancing ×10** (`game/rpgEngine.js` `getPlayerStats` + `data/rpgData.js`): base ATK 30→300, HP 250→2500; nilai 108 field ×10 (WEAPONS.atk, ARMORS.def, MONSTERS.hp/atk/def, RAID_BOSSES.hp/atk/def). Koefisien `*4`/`*2`/`*8` tetap → rasio PvE/PvP tidak berubah; reward/harga/tool bonus tak disentuh.
  - **Raid gear gate** (`handlers/game/rpg/raid.js`): wajib senjata+zirah terpasang, `gearTier = min(tier senjata, tier zirah)` harus ≥ `boss.level − 5` (Naga L13→T8 … Cosmic L20→T15). Diterapkan di `!rpg raid mulai` & `!rpg raid join`.
  - **Raid knock-out**: floor 5% dihapus; HP ≤ 0 saat giliran = GUGUR, pemain dihapus dari raid; semua gugur / < pemain minimum → RAID GAGAL.
  - **Tombol quick reply** (`handlers/game/rpg/commands.js`, `raid.js`): duel challenge → `🤝 Terima`/`❌ Tolak`; prompt giliran duel & raid → `⚔️ Serang`/`🏃 Kabur` (termasuk prompt awal "DUEL DIMULAI" yang sebelumnya teks polos); raid start → `🤝 Gabung`. Semua pakai `sendMenu`+`quickReply`, fallback teks di `BUTTON_MODE=off`.
  - **Cooldown raid dihapus**: hapus `RAID_COOLDOWN` (`data/rpgData.js` + `raid.js`), cek cooldown, dan `setRpgRaid(:cooldown)`; grup bisa langsung raid lagi. Cooldown skill tetap.
  - **`!rpg tempa` jadi dropdown** (`handlers/game/rpg/commands.js`): menu utama ganti teks → `sendMenu` + `listButton` 4 sections (Senjata 20 / Zirah 19 / Kapak 20 / Beliuang 20, total 79 item); tiap row `id = !rpg tempa [alias]` langsung craft; filter kapak/beliuang pakai `startsWith` (sebelumnya `includes('axe')` ikut mencocokkan pickaxe). `!rpg tempa [kategori]` ketik manual tetap jalan.
  - Verifikasi: `node test/smoke.js` 26/26 lulus + test fungsional dropdown/craft mock.
- **15-08-2026** — **Rombak total FishIt: mini-game tarikan bertahap, ekonomi kurva material RPG, harga joran/umpan di-skala ulang**:
  - **Mini-game tarikan bertahap** (`game/fishingEngine.js` `computeTugStages`/`tugWindowMs` + `handlers/game/fishit/commands.js`): mancing bukan lagi "tunggu 5 dtk" — ikan menggigit dalam 2-5 tahap tarikan, tiap tahap kirim tombol `🎣 TARIK!` (`!fishit tarik`), window 7→3 dtk, joran bagus memperlama window (`rodBonus × 8ms`), ikan besar (bobot >1.5× rata-rata tier) tambah 1 tahap. Sukses = semua tahap dilewati → ikan masuk inv/fishdex/achievement (logika lama dipakai ulang); timeout = ikan lepas + umpan habis.
  - **Konsumsi umpan pindah ke awal mancing** (anti-exploit: matikan bot saat nunggu ≠ mancing gratis); `activeFishing` Map diganti `tugState` (Map per-jid, timer TTL auto-bersih ≤30 dtk, dua pemain bisa mancing bareng). Pre-handler `!fishit tarik` di `handlers/game/fishit/router.js` (`sameUser` guard via state key per-jid).
  - **Ekonomi ikut kurva material RPG** (`game/fishingEngine.js` `FISH_SELL_TIERS`): nilai/ekor rata-rata per tier tertambat ke harga jual material (kayu 100 → celestial_core 10jt); tier 4 disetel ke kurva Emas (mult 909→264). `data/gameData.js` — harga joran = 100× nilai/ekor tier (rod_2 20rb … rod_12 1M), umpan = 5× nilai/ekor tier bawah (bait_1 100 … bait_12 25jt). Bobot tetap memodulasi harga.
  - **Migrasi progresi**: flag `p.econV2` — saat pemain pertama mancing, inventory ikan + umpan lama di-reset, joran/fishdex/achievement/totalCaught dipertahankan.
  - **Achievement** (`data/fishitAchievements.js`): reward di-skala ke kurva baru (100rb → 25jt); eval achievement pindah ke titik sukses tarikan.
  - **UI**: menu/help/dropdown diperbarui (mancing = tarikan bertahap), box tahap tarikan baru. Perintah `!fishit` lain tidak berubah.
  - Verifikasi: `node game/fishingEngine.js` self-check OK + `node test/smoke.js` 27/27 lulus (termasuk test fungsional mancing→tarik bertahap→ikan masuk inv).

- **15-08-2026** — **Fitur Gacha Umum `!gacha`**:
  - **Data** (`data/gachaData.js`): harga 50rb/tarik (10x = 500rb), 4 rarity (Common 55% / Rare 30% / Epic 12% / Legendary 3%), pity 50 (tarikan ke-50 dijamin Legendary). Pool hadiah deklaratif per rarity (money, material RPG, item shop/kontrak, ikan FishIt, pokeball, umpan) dengan bobot.
  - **Engine** (`game/gachaEngine.js`): `rollRarity(pity)` (pity force legendary), `pickReward(rarity)` (roll berbobot), `moneyAmount`. Self-check `node game/gachaEngine.js` (pity, distribusi L 2-4.5%, pool valid).
  - **Command** (`handlers/gacha/router.js` + `commands.js`): `!gacha` (menu dropdown), `!gacha 1`, `!gacha 10`, `!gacha info`. Pemberian hadiah via fungsi db existing: money `addMoney`, material RPG root field, item shop `p.inventory`, ikan `pickFish` (bobot acak, `updateFishingPlayer`), pokeball `savePokemonPlayer` (key `resolveNum`), umpan `p.bait`. Auto-register bila player game belum ada.
  - **DB** (`data/db.js`): collection `gachaStats` `{ [jid]: { pity, pulls } }` + `getGachaStats`/`updateGachaStats`/`resetGachaStats` (pity persist → survive restart).
  - **Routing**: `handlers/router.js` daftar `handleGacha` (HANDLERS + `!gacha` di COMMAND_INDEX); `!menu` tambah aksi gacha.
  - Verifikasi: `node test/smoke.js` 32/32 lulus (termasuk test fungsional gacha 1x & 10x).
- **15-08-2026** — **UI: pecah baris box yang render panjang (≥40 kolom) jadi 2 baris gaya gacha**:
  - `handlers/gacha/commands.js` — harga 1x/10x di box info & box "money tidak cukup" dipisah baris (baris 2 indentasi).
  - `handlers/game/rpg/commands.js` — profil: baris stat Crit/Block/Reward dipecah; `materialLine` di-chunk 2 item/baris (`materialLines`); list fusi: baris esensi dipecah (nama→target terpisah).
  - `handlers/game/rpg/classSkills.js` — `!rpg kelas`: Biaya money + kontrak kelas dipisah baris.
  - `handlers/game/casino/commands.js` — list material judi & contoh perintah dipecah 2 baris.
  - `handlers/economy/claim.js` — teks JACKPOT dipisah baris (nilai jual di baris lanjutan).
  - `handlers/game/togel/commands.js` — note timer dibuka otomatis dipecah.
  - `handlers/game/rpg/raid.js` — log timeout raid dipecah 2 baris.
  - `handlers/pet/commands.js` — bullet evolusi (syarat + hasil) dipecah.
  - Verifikasi: `node test/smoke.js` 32/32 lulus.
- **16-08-2026** — **Rename `ADMIN_JID` → `OWNER_JID` + peran admin grup (owner vs admin grup dibedakan)**:
  - **Rename global**: `ADMIN_JID` → `OWNER_JID` di semua kode (`.env`, `.env.example`, `index.js`, handler, `test/smoke.js`, `setup-windows.bat`, `setup-termux.sh`); `handlers/admin/adminCommands.js` → `handlers/owner/ownerCommands.js` (require di `handlers/router.js` diupdate). Pesan tolak jadi "hanya untuk owner bot".
  - **Peran admin grup** (`utils/jid.js` `isGroupAdmin`): cek `sock.groupMetadata(chatId).participants[].admin` ('admin'/'superadmin'), alias-aware via `resolveNum`.
  - **Ban lokal per grup** (`data/db.js`): collection `groupBans` `{ [groupId]: { [jid]: { bannedAt, bannedBy } } }` + `banUserInGroup`/`unbanUserInGroup`/`isUserBannedInGroup`/`getGroupBannedList`. `index.js` cek `isUserBannedInGroup(groupId, senderJid)` → user ban lokal tidak bisa pakai fitur bot di grup itu, normal di grup lain.
  - **Izin** (`handlers/owner/ownerCommands.js`): owner = semua + ban global; admin grup = `!ban`/`!unban`/`!banlist` lokal per grup (target wajib member biasa — admin lain/owner/bot ditolak), `!del`, `!hidetag`. `!ban all`/`!unban all`/`!allowgroup`/`!acc`/`!reset` tetap owner-only.
  - Verifikasi: `node test/smoke.js` 37/37 lulus (termasuk test baru izin owner vs admin grup).
- **16-08-2026** — **Hapus mini-game tarikan bertahap FishIt, kembali ke pola tunggu 5 detik**:
  - `game/fishingEngine.js` — hapus `computeTugStages`/`tugWindowMs`/`avgWeightForTier` + export + self-check; sisa `getFishingDelay()` (5000 ms) dipakai untuk jeda mancing.
  - `handlers/game/fishit/commands.js` — `tugState` + `sendTugStage` + `handleTug` dihapus, ganti `activeFishing` Map (guard mancing berlangsung). `cmdMancing` alur baru: konsumsi 1 umpan sekarang (anti-exploit) → kirim box "X sedang memancing di <lokasi>!" → `setTimeout(getFishingDelay())` → roll tier → zonk (pesan acak) **atau** tangkapan (box TANGKAPAN + Mancing Lagi). Keduanya muncul setelah 5 detik.
  - `handlers/game/fishit/router.js` — hapus `case 'tarik'`; help & menu diperbarui (tanpa narasi tahap tarikan).
  - `test/smoke.js` — TEST 4 tulis ulang: mancing → pesan terkirim + `activeFishing` aktif → tunggu delay+buffer → hasil masuk inv / umpan berkurang.
  - Verifikasi: `node test/smoke.js` 37/37 lulus.
- **16-08-2026** — **Fitur baru: `!bot` AI asisten owner (DeepSeek V4 Flash free via 9router)**:
  - `handlers/ai/commands.js` (baru) — `handleBot`: owner-only (cek `sameUser`), tanpa argumen → bantuan cara pakai, ada argumen → POST `${DEEPSEEK_BASE_URL}/chat/completions` via axios (model `DEEPSEEK_MODEL`, `max_tokens:1024`, timeout 120s, indikator composing). Ambil `choices[0].message.content`, fallback `reasoning_content` (model thinking). Strip trailing `data: [DONE]` di respons (gotcha 9router). Potong jawaban >4000 char. Cooldown in-memory 3s/chat anti-spam.
  - `handlers/router.js` — require `handleBot`, tambah ke `HANDLERS` + `['!bot', handleBot]` di `COMMAND_INDEX`.
  - `.env` / `.env.example` — `DEEPSEEK_API_KEY`, `DEEPSEEK_BASE_URL` (default `http://localhost:20128/v1`), `DEEPSEEK_MODEL` (default `oc/deepseek-v4-flash-free`).
  - Endpoint diuji manual (curl/Invoke-RestMethod): model `oc/deepseek-v4-flash-free` merespons, cost 0. `!bot` sengaja TIDAK masuk test smoke (butuh network; sandbox offline).
  - Verifikasi: `node test/smoke.js` lulus (handler baru tidak merusak route).
- **16-08-2026** — `!bot`: tambah system prompt supaya jawaban AI dalam Bahasa Indonesia singkat/padat/jelas + emoticon minimal (diuji live, respons sesuai).
- **16-08-2026** — `!bot`: ganti persona AI jadi cewek anime tsundere (malu-malu sok dingin, "b-baka!", "jangan salah paham!"), Bahasa Indonesia singkat, diuji live respons sesuai.
- **16-08-2026** — `!bot` dapat perintah natural-language owner (parser lokal + fallback AI):
  - `handlers/ai/commands.js` — `parseIntent(prompt)` terjemah bahasa alami → command existing, dipanggil via `{...ctx, body}` (logika handler lama dipakai ulang, tidak diduplikasi). Pola: `izinkan grup ini <durasi>`→`!allowgroup <durasi>` (durasi via `parseDurasi`: 1 hari→1d, 2 jam→2h, 1 minggu→7d, permanen→forever), `daftar grup diizinkan`→`!allowgroup list`, `cabut izin grup <id>`→`!allowgroup remove`, `ban @user`/`unban @user`→`!ban`/`!unban` (mention tetap terbaca dari msg), `ban semua`→`!ban all`, `daftar banned`→`!banlist`, `reset akun rpg/fishit/pokemon/money`→`!reset`, `izinkan getpp @user`→`!acc`, `cabut izin getpp`→`!unacc`. Tak cocok pola → fallback AI chat. Tetap owner-only.
  - `handlers/owner/ownerCommands.js` — `resolveFlexDuration` support durasi bebas `<angka><d|h|m|w>` di `!allowgroup` (mis. `1d`).
  - Verifikasi: `node test/smoke.js` 37/37 lulus; check parser 24 kasus (intent + durasi) lulus; jalur list/owner/no-arg diuji via mock sock. Jalur penulis `allowed_groups.json` sengaja tak diuji di smoke (file tak punya override sandbox).
- **16-08-2026** — `!bot` bisa merespons pesan yang di-reply:
  - `handlers/ai/commands.js` — helper `getQuotedText(qm)` ekstrak teks dari `quotedMessage` (conversation / extendedTextMessage / caption image-video-document-audio). Di `handleBot`: bangun objek `quote` dari `contextInfo` (pola `!hidetag`/`!send`); semua kirim pesan pakai `{ quoted: quote }` → jawaban meng-quote pesan yang di-reply (bukan perintah). `effectivePrompt = prompt || getQuotedText(...)` → `!bot` tanpa teks + reply = prompt isi pesan yang di-reply; dengan teks = prompt tetap teks tapi tetap quote pesan reply. Help text +baris "Balas pesan + ketik !bot → AI menjawab isi pesan itu".
  - `test/smoke.js` — TEST 5b `testBotReplyQuote`: owner reply pesan + bare `!bot` → `lastSendOpts.quoted.key.id === stanzaId` & `quoted.message.conversation === quotedText` (quote pesan target, bukan perintah).
  - Verifikasi: `node test/smoke.js` 38/38 lulus.
- **16-08-2026** — `!bot` fix jawaban terpotong / isi berpikir model bocor:
  - `handlers/ai/commands.js` — `max_tokens` 1024→4096 (thinking model menghabiskan jatah token buat `reasoning_content` → `content` final kosong). Buang fallback `reasoning_content` sebagai jawaban (itu isi berpikir internal, bukan jawaban; panjang → kena potong `MAX_CHARS`). Kalau `content` kosong → pesan "🤔 Model tidak menghasilkan jawaban. Coba pertanyaan lain...".
  - Verifikasi: `node test/smoke.js` 38/38 lulus; jawaban `!bot` live tidak lagi menampilkan "… *(dipotong, jawaban terlalu panjang)*".
- **17-08-2026** — **RPG: semua tempa pedang/cangkul/kapak wajib kayu + kayu tidak bisa dibeli**:
  - `data/rpgData.js` — `wood.canBuy: true` → `false` (otomatis hilang dari dropdown toko material & `!rpg shop material` filter `canBuy`; `!rpg beli kayu` ditolak di `handleRpgBuy`). Jual kayu tetap bisa (`sellPrice` utuh).
  - `data/rpgData.js` — semua `craftCost` di `WEAPONS` (20) + `TOOLS` axe (20) + pickaxe (20) ditimpa/ditambah `wood: 5 + tier*5` (T1=10, T2=15, ..., T20=105), progresi monoton. Resep lama yang sudah punya wood di-override agar seragam. `ARMORS` tidak disentuh.
  - Verifikasi: `node -e` cek semua 60 resep punya `wood` & monotonic + `wood.canBuy===false`; `node test/smoke.js` 40/40 lulus.
- **17-08-2026** — **Rilis resmi versi 1.10**:
  - Penanda versi diselaraskan: judul & "Versi saat ini" `CHANGELOG.md` → 1.10 (sebelumnya tertinggal 1.4), `AGENTS.md` status "beta" ditutup, `package.json` `description` diisi, `package-lock.json` root version disamakan ke 1.10.0.
  - Tidak ada perubahan logika fitur; verifikasi `node test/smoke.js` lulus.
- **17-08-2026** — **Fitur baru: `!top bust` — top 5 kerugian Blackjack per grup**:
  - `data/db.js` — collection baru `bjStats` `{ [groupId]: { [jid]: { loss } } }` + `recordBjLoss(groupId, jid, amount)` (akumulasi uang hilang saat kalah) + `getBjTop(groupId, limit)` (sort desc loss, lampirkan nama dari fishingPlayers/rpgPlayers/pokemonPlayers, pola `getTopGlobalMoney`).
  - `handlers/game/casino/commands.js` — `resolveBlackjack` catat `recordBjLoss(chatId, loserJid, bet)` saat ada pemenang (seri/timeout tidak dicatat). Handler `handleTopBust(ctx)`: box `🏆 TOP BUST GRUP`, 5 besar `@nomor — 💸 x money`, kirim `mentions` real JID.
  - `handlers/game/casino/router.js` — `handleTopBustRouter` cek body `!top bust`. `handlers/router.js` register ke array `HANDLERS` (fallback; token `!top` sudah diklaim `handleTopMoney` yang return false untuk body lain).
  - `handlers/utility/menuCommands.js` — `!top bust` ditambah di teks & dropdown menu.
  - `test/smoke.js` — TEST 7 `testTopBust`: tanpa data → pesan kosong; setelah `recordBjLoss` → box berisi TOP BUST + @nomor + jumlah + `mentions`.
  - Verifikasi: `node test/smoke.js` lulus.
- **17-08-2026** �?" **Fitur baru: ingatan `!bot` (owner PM) — fakta permanen + riwayat chat**:
  - `handlers/ai/memory.js` (baru) �?" state JSON sinkron `ai_memory.json` (env override `BOT_AI_MEMORY_PATH`). API: `getFacts`/`addFact` (dedup)/`removeFact` (hapus yang mengandung kata kunci)/`getHistory`/`pushHistory` (cap 10 pesan + 8000 char biar hemat token model free). File korup �+' fallback kosong.
  - `handlers/ai/commands.js` �?" `parseIntent` +3 pola natural-language: `ingat bahwa <fakta>` / `tolong ingat <fakta>` (simpan fakta mentah, konfirmasi `✅ Diingat`), `lupakan <kata>` / `lupa <kata>` (hapus fakta cocok), `daftar ingatan` / `apa yang kamu ingat` (list fakta). Handler baru `handleMemoryIntent` (baca body dari `ctx.body`, pola intent handler existing). Saat AI chat: fakta di-inject ke system prompt + 10 pesan riwayat terakhir dikirim sebagai konteks; jawaban sukses di-append ke riwayat. Help text `!bot` +daftar contoh ingatan.
  - `test/smoke.js` �?" env `BOT_AI_MEMORY_PATH` ke sandbox + TEST 8 `testBotMemory` (5 cek: simpan/list/hapus via route PM owner, parser kenali pola, riwayat cap 10).
  - Verifikasi: `node test/smoke.js` 47/47 lulus.
- **20-08-2026** — **Fitur baru: game Tebak Boom `!tebakboom`**:
  - `handlers/game/minigame/boom.js` (baru) — 25 kotak, 10 bom posisi acak, multiplayer taruhan. Perintah: `!tebakboom <jumlah>` (buat room, bet fixed utk semua, saldo pembuat langsung dipotong), `!tebakboom join`, `!tebakboom start` (min 2 pemain, urutan giliran diacak), `!tebakboom open <n>` / `!buka <n>` (alias token `!boom`), `!tebakboom cancel` (refund semua). Giliran dijaga `sameUser`; bom → gugur. Akhir: 15 kotak aman terbuka → semua survivor menang, pot dibagi rata (`Math.floor`, sisa ke pemenang pertama); semua gugur → taruhan hangus, tidak ada pemenang. TTL 10 mnt (pola blackjack) → refund otomatis. Hanya di grup.
  - `handlers/router.js` — `handleBoom` masuk `HANDLERS` + token `!tebakboom`/`!boom`/`!buka` di `COMMAND_INDEX`.
  - `test/smoke.js` — TEST 9 `testBoom`: create/join potong saldo, start, `!buka 1` giliran aktif merespons.
  - Verifikasi: `node test/smoke.js` lulus.
- **20-08-2026** — **Tebak Boom: tombol quick reply `Join` / `Mulai`**:
  - `handlers/game/minigame/boom.js` — pesan buat room & join kini via `sendMenu` dengan `quickReply('!tebakboom join')` / `quickReply('!tebakboom start')` (pola gacha/router.js); klik tombol = id command di-bridge jadi body. `fallbackText` berisi teks asli + perintah ketik manual → tetap sampai saat tombol gagal/BUTTON_MODE=off. `MAX_PLAYERS` 15→10 (sesuai jumlah bom).
  - Verifikasi: `node test/smoke.js` lulus (mock fallback teks tetap lolos).
- **20-08-2026** — **Tebak Boom: giliran buka kotak via dropdown**:
  - `handlers/game/minigame/boom.js` — pesan `start` (giliran pertama) & pesan lanjut giliran kini `sendMenu` + `listButton('dY'� Pilih Kotak', boxSections(game))`. Helper `boxSections` menyusun sections per baris 5A-5 berisi row `Kotak <n>` �+' id `!buka <n>`; kotak yang sudah terbuka di-skip (dropdown menyusut tiap giliran). Klik row = di-bridge jadi body; guard `sameUser` tetap tolak pemain bukan-giliran. `fallbackText` berisi instruksi ketik manual.
  - Verifikasi: `node test/smoke.js` lulus (mock fallback teks tetap lolos).
- **20-08-2026** — **Rilis resmi versi 1.12**:
  - Penanda versi diselaraskan: judul & "Versi saat ini" `CHANGELOG.md` → 1.12, `AGENTS.md` → 1.12, `package.json` `name`/`version` + `package-lock.json` root → 1.12.0 (folder proyek sudah 1.12 sejak awal, penanda internal tertinggal 1.10).
  - Isi rilis ini (fitur sejak 1.10, sudah tercatat di Riwayat di atas): RPG tempa wajib kayu & kayu tak bisa dibeli (17-08), `!top bust` (17-08), ingatan `!bot` fakta permanen + riwayat chat (17-08), game **Tebak Boom** `!tebakboom` + quick reply `Join`/`Mulai` + dropdown buka kotak (20-08).
  - Tidak ada perubahan logika fitur; verifikasi `node test/smoke.js` lulus.

- **20-08-2026** — **Ganti trigger AI: prefix `!bot` → free-text owner berakhiran titik `.`**:
  - `handlers/ai/commands.js` — trigger baru: free-text owner (PM & grup) yang diakhiri titik (regex `/\.\s*$/`, toleransi spasi) → AI; titik di-strip dari prompt sebelum `parseIntent` & sebelum dikirim ke model. Fitur AI khusus owner — non-owner `return false` (PM & grup). `!xxx` di PM owner → tetap info sewa; `!xxx` di grup → diabaikan. Reply pesan + `.` / teks bertitik → AI baca isi pesan yang di-reply (tetap didukung). Teks panduan diperbarui (contoh `halo apa kabar.`, `izinkan grup ini 1 hari.`).
  - `handlers/router.js` — token `!bot` dihapus dari `COMMAND_INDEX`; `handleBot` tetap terakhir di `HANDLERS` (fallback free-text).
  - `handlers/ai/memory.js` & `handlers/owner/ownerCommands.js` — komentar menyesuaikan.
  - `test/smoke.js` — TEST 5b body `'!bot'`→`'.'` (reply+titik), TEST 5c `'lagi apa?'`→`'lagi apa.'`, TEST 8 semua body intent +titik; label & komentar disesuaikan.
  - Verifikasi: `node test/smoke.js` lulus.

- **21-08-2026** — **Fix reconnect kadang tidak benar-benar menyambung ulang**:
  - `index.js` — semua penyambungan ulang kini lewat helper `scheduleReconnect(reason)`: guard anti dobel jadwal (`reconnectTimer`), backoff eksponensial 3 dtk → maks 5 mnt, dan `startBot()` yang gagal saat reconnect (internet masih mati → fetch versi WA gagal, file session terkunci) otomatis dicoba ulang dengan jeda makin panjang. Sebelumnya: `setTimeout(startBot)` telanjang — sekali gagal = bot diam permanen, dan rejection tak tertangani bisa mematikan proses (default Node ≥15).
  - Guard generasi socket (`sessionGen`): listener `connection.update` / `messages.upsert` / `creds.update` dari sesi lama diabaikan setelah reconnect → socket "hantu" tidak lagi memicu reconnect dobel atau pesan diproses 2x. Socket lama di-`end()` sebelum mulai ulang. Backoff direset ke 3 dtk tiap `connection === 'open'`.
  - Net proses: `process.on('unhandledRejection')` + `'uncaughtException'` → cukup log, proses tetap hidup.
  - Verifikasi: `node --check index.js` + `node test/smoke.js` 52/52 lulus.

- **21-08-2026** — **Trigger AI dibedakan PM vs grup**:
  - `handlers/ai/commands.js` — PM owner: free-text langsung diproses AI tanpa akhiran titik; grup tetap wajib akhiran titik `.` (cek jadi `isGroup && !endsWithDot`). Titik tetap di-strip bila ada. Fitur AI khusus owner (guard `sameUser(senderJid, OWNER_JID)` tidak berubah). Teks panduan AI diperbarui ("Di PM cukup kirim pesan langsung; di grup akhiri dengan titik").
  - `test/smoke.js` — +2 cek: PM owner free-text tanpa titik → AI merespons; grup owner free-text tanpa titik → tidak diproses AI.
  - Verifikasi: `node test/smoke.js` lulus semua.

- **22-08-2026** — **Casino: banner gambar + `!casino` jadi menu, taruhan money pindah `!casino money`**:
  - `handlers/game/casino/commands.js` — banner `bannercasino.png` di root dibaca sekali & di-cache (`casinoBanner()`); file hilang → semua kirim fallback teks biasa. Helper `sendCasinoMsg(sock, chatId, text, quoted)` mengirim `{ image, caption }` saat banner ada.
  - `!casino` tanpa perintah dikenali → **menu casino** via `sendMenu` dengan `headerImage` (banner jadi header pesan interaktif) + dropdown "Pilih Game": Roll Money (`!casino money`), Material RPG (`!casino material`), Ikan FishIt (`!casino fish`), Panduan Lengkap (`!casino help`). Fallback tombol gagal / `BUTTON_MODE=off` → otomatis gambar + caption menu.
  - Taruhan money pindah ke sub-perintah: `!casino money [jumlah|all]` (alias `uang`). Kompatibilitas lama tetap: `!casino <angka>` / `!casino all` masih bet money.
  - Hasil roll money, material, ikan, dan panduan (`!casino help`) kini dikirim dengan banner sebagai gambar + caption. Blackjack tetap teks (pesan status dinamis & sering).
  - `showCasinoHelp` diperbarui (`!casino money [jumlah]`, dsb.). Router tidak berubah.
  - Verifikasi: `node test/smoke.js` lulus.

- **22-08-2026** — **Dropdown `!casino` dihapus → arahan cara pakai; banner di hasil Blackjack & Togel**:
  - `handlers/game/casino/commands.js` — menu dropdown "Pilih Game" pada `!casino` dihapus; tanpa perintah yang dikenali kini langsung kirim panduan (`showCasinoHelp`) via `sendCasinoMsg` (banner + caption). Import `listButton` dibuang (tak terpakai lagi di file ini; blackjack tetap pakai quick-reply).
  - Helper `sendCasinoMsg(sock, chatId, text, quoted, mentions)` diperluas dengan parameter opsional `mentions`. **Penting: mentions ditaruh di objek content** (`{ image, caption, mentions }` / `{ text, mentions }`), bukan di options ke-3 `sendMessage()` — Baileys hanya membaca `message.mentions` dari content (`node_modules/@whiskeysockets/baileys/lib/Utils/messages.js`); lewat options, tag `@nomor` jadi teks biasa tanpa highlight. Berlaku untuk teks maupun gambar+caption.
  - Hasil akhir Blackjack (`resolveBlackjack`) kini dikirim via `sendCasinoMsg` + mention kedua pemain (sebelumnya teks polos). Timeout/expire BJ tetap teks polos.
  - `sendCasinoMsg` kini diexport dari `casino/commands.js`; `handlers/game/togel/commands.js` mengimpornya — hasil togel (`executeDraw`: auto-draw timer & `!togel buka` admin) kini tampil dengan banner + mention pemenang. Menu/list/riwayat/top togel tetap teks polos.
  - Verifikasi: `node test/smoke.js` 54/54 lulus.

- **26-08-2026** — **Port fitur fun & tools dari NexaBot (folder `bot/`) — 8 command stateless**:
  - File baru `handlers/nexa/commands.js` — porting plugin pilihan dari project NexaBot (ESM → CommonJS, disesuaikan konvensi handler `ctx` + return true): `!jokes`/`!lucu` (lawak acak), `!quote`/`!kutipan` (kutipan motivasi), `!dare`/`!tantangan`, `!truth`/`!jujur`, `!8ball`/`!magic` (wajib pertanyaan), `!dice`/`!roll` (1–6 dadu, guard jumlah: non-bilangan/negatif → default 1), `!base64`/`!b64` (enc/dec), `!calc`/`!kalkulator`/`!math` (kalkulator). Semua stateless: tanpa DB, tanpa API eksternal; data array di-inline di file.
  - Keamanan `!calc`: ekspresi divalidasi regex `^[0-9+\-*/%().\s]+$` (tanpa huruf = tak bisa memanggil fungsi) sebelum dievaluasi via `Function`; koma desimal → titik.
  - `handlers/router.js` — 8 handler direquire, didaftarkan di `COMMAND_INDEX` (17 token) + array `HANDLERS`.
  - Skip dari NexaBot: fitur ber-API eksternal (lyrics, translate, weather, tts, meme, qr, ai) dan fitur berstate (coin, slot, quiz, mathquiz, tebakangka, note) — bisa menyusul bila dibutuhkan.
  - `test/smoke.js` — TEST 10 baru: 15 cek (respons 8 command + alias, 8ball tanpa/dengan argumen, dice default & guard negatif, round-trip base64, calc benar & injeksi teks ditolak). Total 69 cek.
  - Verifikasi: `node test/smoke.js` 69/69 lulus.

- **26-08-2026** — **Fitur `!jadian` (matchmaker grup)**:
  - `handlers/nexa/commands.js` — `handleJodoh`: hanya di grup; ambil `sock.groupMetadata(groupId)`, filter peserta selain pengirim & bot (`sameUser`), pilih 1 acak lalu kirim `💞 @sender jadian sama @target 💞` dengan `mentions: [senderJid, targetJid]`. Kosong/tidak ada kandidat → pesan ramah; PM → "cuma di grup". `groupMetadata` di-wrap try/catch.
  - Token: `!jadian` / `!jodoh` / `!jodian` (alias typo umum) di `COMMAND_INDEX` + `HANDLERS` (`handlers/router.js`).
  - `test/smoke.js` — TEST 11 baru: jodoh mention sender + 1 member, dan PM ditolak. Total 71 cek.
  - Verifikasi: `node test/smoke.js` 71/71 lulus.

- **26-08-2026** — **Game `!tebak` (tebak angka 1-100, reward money, input via reply)**:
  - `handlers/nexa/commands.js` — `handleTebakAngka`: main gratis, menang dapat **+1000 money** (reward tetap via `addMoney`, bebas exploit parsing uang). State per-chat `tebakGames` Map (`chatId` → `{ number, tries, msgId, createdAt }`) + TTL 5 menit (prune saat start). `!tebak`/`start` → bot pilih 1-100 & kirim prompt (simpan `msgId` balasan); `!tebak stop` → hapus sesi. Tebak via **reply** ke pesan prompt (`extendedTextMessage.contextInfo.stanzaId === g.msgId`, isi hanya digit) **atau** `!tebak <n>`. Guard `Number.isInteger(n) && n>=1 && n<=100` (bukan `parseInt||1`). Benar → `addMoney(sender, 1000)` + hapus; salah → hint `⬆️/⬇️`.
  - Token: `!tebak` / `!tebakangka` di `COMMAND_INDEX` + `HANDLERS` (`handlers/router.js`). Tak bentrok dgn `!tebakboom` (token beda).
  - `test/smoke.js` — TEST 12 baru: start & prompt 1-100, guard `-5`/`abc`, tebak via reply pesan prompt, loop 1..100 → menang & money +1000. Total 75 cek.
  - Verifikasi: `node test/smoke.js` 75/75 lulus.

- **29-08-2026** — **Fitur `!sw` (bot pasang WhatsApp Status)**:
  - `handlers/owner/ownerCommands.js` — `handleSw`: akses owner atau admin grup (pola `!hidetag`/`!del`). `!sw <teks>` → `sendMessage('status@broadcast', { text })`. Reply foto/video lalu `!sw [caption]` → `downloadMediaMessage` (pola `!send`) → kirim `image:`/`video:` ke `status@broadcast` (tanpa re-encode ffmpeg). Tanpa teks & tanpa reply media → pesan cara pakai. Konfirmasi + try/catch error di grup.
  - Token `!sw` di `COMMAND_INDEX` (`handlers/router.js`).
  - `test/smoke.js` — TEST 5d(e): admin grup boleh pasang status (`sentTo` includes `status@broadcast`), member biasa ditolak.
  - Verifikasi: `node test/smoke.js` 77/77 lulus.

- **29-08-2026** — **FIX `!sw` → Group Status (status muncul DI DALAM grup, bukan di nomor bot)**:
  - User mau "grup buat story" = WhatsApp **Group Status**. Baileys v7 ter-install hanya bisa DECODE `groupStatusMessage` (WAProto ada), tapi `generateWAMessageContent` tak punya branch generate-nya → `sendMessage` biasa tak bisa.
  - `handleSw` kini kirim group status ke `chatId` (grup) via build manual: `generateWAMessageContent(inner)` → `proto.Message.create({ groupStatusMessage: { message: proto.Message.toObject(inner) } })` (plain object, bukan instance, agar `generateWAMessageFromContent` tak hilangkan field) → `generateWAMessageFromContent` → `sock.relayMessage(chatId, message, { messageId })`.
  - Text: `inner = { text, backgroundColor:'#25D366', font:1 }`. Media (reply foto/video): `downloadMediaMessage` buffer → `inner = { image/video: buffer, caption }` (upload via `sock.waUploadToServer`).
  - Gate: owner/admin grup + wajib di grup (group status WA butuh bot jadi admin grup utk posting).
  - `test/smoke.js` — `relayMessage` dipasang lokal di tes; assert dipanggil ke grup dgn `groupStatusMessage` berisi teks. 78/78 lulus.

- **29-08-2026** — **FIX `!sw` → ganti Baileys ke fork `@nexustechpro/baileys` (native group status)**:
  - `@whiskeysockets/baileys@^7.0.0-rc14` resmi TIDAK bisa generate group status (cuma decode). Build manual relay gagal di produksi. Ganti dependency ke fork **`@nexustechpro/baileys@^2.2.6`** via alias npm: `"@whiskeysockets/baileys": "npm:@nexustechpro/baileys@^2.2.6"` (import `require('@whiskeysockets/baileys')` tetap jalan, drop-in 7.x).
  - `handleSw` disederhanakan: buang helper relay manual + import `proto/generateWAMessageContent/generateWAMessageFromContent`. Teks → `sock.sendGroupStatusMessage(chatId, { text, backgroundColor:'#25D366', font:1 })`; media (reply foto/video) → `downloadMediaMessage` buffer → `sock.sendGroupStatusMessage(chatId, { image/video: buffer, caption })`. Nexus tangani encode + relay ke grup.
  - `sharp` naik `^0.35.4` + `overrides.sharp` (nexus tarik `wa-sticker-formatter` yg butuh `sharp@0.30.7` native rusak di Node 26; override paksa semua sharp ke 0.35.4 yg load). `allowScripts` ditambah utk nexus/sharp/protobufjs/ffmpeg-static/sqlite3/whatsapp-rust-bridge/rlottie.
  - `test/smoke.js` — mock `sendGroupStatusMessage`; assert dipanggil ke grup dgn `data.text`. 78/78 lulus.

- **29-08-2026** — **FIX `!s`/`!sticker` untuk VIDEO (pakai `wa-sticker-formatter`)**:
  - Cabang video `handleSticker` (`handlers/media/commands.js`) tadinya pakai ffmpeg filter `libwebp` tangan sendiri (crop/scale/fps/loop) → rapuh, gagal di video berotasi/aspek aneh. Diganti ke `new Sticker(tmpIn, { pack:'Bot WhatsApp', author:'Bot', type:'crop', quality:80 }).build()` dari `wa-sticker-formatter` (sudah ada sbg transitive nexus, dijadikan direct dep `^4.4.4`). Library tangani crop persegi + loop + exif + animated webp dgn benar.
  - `handlers/media/download.js` — set `process.env.FFMPEG_PATH`/`FFPROBE_PATH` global agar spawn ffmpeg dari `wa-sticker-formatter` (instans fluent-ffmpeg sendiri) pakai binary yg sama dgn bot.
  - Cabang gambar tetap pakai `makeWebpSticker` (utils/sharp) utk risiko minimal.
  - Verifikasi: `node test/smoke.js` 78/78 lulus (konversi video e2e butuh ffmpeg yg tak ada di sandbox → uji real-run di bot).

- **29-08-2026** — **REVERT `!s` video → `wa-sticker-formatter` DIBATALKAN (crash `-vsync`)**:
  - `wa-sticker-formatter@4.4.4` manggil ffmpeg dgn opsi `-vsync` yg **sudah dibuang di ffmpeg 7+** → `Error splitting the argument list: Option not found` (crash di env ffmpeg user yg baru). Library itu incompatible dgn ffmpeg modern. Hapus dep + import-nya.
  - Webp-muxer ffmpeg (encoder `libwebp`) TANPA `-fps_mode passthrough` → webp **STATIS** (ffmpeg buang timestamp). GIF→sharp (`animated:true`, teruji chunk `VP8X`+`ANIM` di sandbox) juga statis di env user utk durasi panjang.
  - **FIX final**: ffmpeg langsung ke `libwebp` + **`-fps_mode passthrough`** (ganti `-vsync 0`, compat ffmpeg 7) + `-loop 0` → seluruh frame lewat, webp bergerak. Durasi cap **10 detik**, fps 10, square 512×512. `process.env.FFMPEG_PATH`/`FFPROBE_PATH` di `download.js` tetap dipertahankan.
  - `node test/smoke.js` 78/78 lulus.

- **31-08-2026** — **Penyempurnaan Tampilan UI, Framing Box & Sinkronisasi Menu**:
  - `utils/ui.js` — `ui.box()` kini menambahkan garis penutup panel bawah simetris (`╰━━━━━━━━━━━━━━━━╯`) agar semua pesan berbingkai (RPG, FishIt, Pokemon, Profil, Menu) tertutup rapi di chat WhatsApp.
  - `handlers/utility/menuCommands.js` — sinkronisasi kategori `!menu`: penambahan seksi `🎉 FUN & SOSIAL` (`!jadian`, `!jokes`, `!quote`, `!truth`/`!dare`, `!8ball`, `!dice`), pembaruan kategori `🕹️ MINI GAME` (`!tebak`, `!tebakboom`), dan tools baru (`!calc`, `!b64`) di pesan menu & dropdown.
  - `handlers/nexa/commands.js` — perapihan visual & layout pada command `!jokes`, `!quote`, `!dare`, `!truth`, `!8ball`, `!dice`, `!base64`, `!calc`, `!jadian`, `!tebak` agar seragam & enak dibaca.
  - Verifikasi: `node test/smoke.js` 78/78 lulus.

- **01-09-2026** — **Peningkatan UI & UX Menyeluruh (Tampilan Menu, Profil, Game & Gacha)**:
  - `utils/ui.js` — Perapihan format footer styling, penambahan helper `badge()`, dan optimasi padding visual.
  - `handlers/utility/menuCommands.js` — Desain ulang teks & deskripsi dropdown `!menu` dan `!game` agar lebih informatif, padat, dan rapi.
  - `handlers/economy/profile.js` — Pembaruan tampilan profil (`!me`) dan peringkat terkaya (`!top money`) dengan format visual baru & info aksi cepat.
  - `handlers/game/casino/commands.js` — Perapihan format tampilan `!top bust` (loss Blackjack).
  - `handlers/gacha/commands.js` — Peningkatan estetika hasil tarikan gacha (`!gacha 1`/`!gacha 10`) dan info rate pity.
  - Verifikasi: `node test/smoke.js` 90/90 lulus.

- **01-09-2026** — **Penyatuan Menu & Dropdown Pokemon (`!p menu`) + Quick Reply Action**:
  - `handlers/game/pokemon/commands.js` & `handlers/game/pokemon/router.js` — Penyatuan pengiriman teks menu Pokemon dengan dropdown tombol interaktif dalam 1 pesan via `sendMenu` (mengikuti standar 1 pesan per menu).
  - Penambahan tombol quick reply interaktif pada event game Pokemon:
    - **Spawn (`!p spawn`)**: Quick button `⚔️ Serang`, `🎣 Tangkap`, `🏃 Kabur`.
    - **Battle Liar (`!p serang`)**: Quick button `⚔️ Serang Lagi`, `🎣 Lempar Ball`, `🏃 Kabur`.
    - **Hasil Tangkapan (`!p tangkap`)**: Quick button `✨ Spawn Lagi`/`⚔️ Serang Dulu`, `🎣 Tangkap Lagi`, `🎒 Koleksi`/`👤 Profil` (berlaku untuk tangkapan baru, tangkapan duplikat berkoin kompensasi, maupun tangkapan gagal).
    - **Duel PvP (`!p battle` & `!p lawan`)**: Tombol mulai battle `💥 Serang Lawan` & `🏳️ Batalkan Duel`, tombol kelanjutan giliran `💥 Serang` & `🏳️ Batal`, serta navigasi pasca duel selesai (`✨ Cari Pokemon`, `👥 Susunan Tim`, `🏆 Peringkat`).
    - **Wild Pokemon Pingsan**: Ketika musuh liar pingsan (catch rate maksimal 98%), bot menyertakan quick reply `🎣 Tangkap Sekarang` & `🏃 Tinggalkan`.
  - Verifikasi: `node test/smoke.js` 90/90 lulus.

- **01-09-2026** — **Quick Reply & Dropdown Interaktif: FishIt, RPG, Pokemon Profil/Koleksi**:
  - `handlers/game/fishit/router.js` — Quick reply pada `!fishit profil` (`🎣 Mancing Spot 1`, `🎒 Inventori`, `💸 Jual Semua`) dan `!fishit inv` (`💸 Jual Semua`, `🎣 Mancing`, `🏪 Toko`).
  - `handlers/game/rpg/commands.js` — Quick reply pada `!rpg profil` (`🪓 Tebang`, `⛏️ Tambang`, `👹 Lawan Monster`); `!rpg inv` (`⚒️ Tempa`, `💸 Jual Material`, `🏪 Toko`); hasil battle PvE (`👹 Lawan Lagi` / `🧪 Pakai Ramuan`, `🏪 Beli Ramuan`, `👤 Profil`). Dropdown pada `!rpg equip` tanpa argumen (daftar equipment dimiliki dengan shortcut pasang). `!rpg equip [nama]` hasil sukses juga dapat quick reply navigasi.
  - `handlers/game/pokemon/commands.js` — Quick reply pada `!p profil` (`✨ Cari Pokemon`, `🎒 Koleksi`, `🏆 Peringkat`) dan `!p koleksi` (`✨ Spawn Pokemon`, `👥 Atur Tim`, `🏪 Toko`).
  - Verifikasi: `node test/smoke.js` 90/90 lulus.

- **02-09-2026** — **Reorganisasi struktur folder root** (tanpa ubah logika):
  - Baru: `scripts/` (start.bat, setup-termux.sh, setup-windows.bat, install-ffmpeg.bat), `assets/` (bannercasino.png, my.png, ulartangga.png), `state/` (database.json, afk_state.json, family100_state.json, ai_memory.json, allowed_groups.json).
  - `scripts/start.bat` — fix base dir `%~dp0\..` agar `npm start` jalan dari root (sebelumnya ad di scripts/).
  - Update path default: `data/db.js`, `handlers/utility/utilityCommands.js`, `handlers/game/minigame/family100.js`, `handlers/ai/memory.js`, `utils/groupAccess.js`, `handlers/game/casino/commands.js`, `handlers/game/minigame/snakes.js`.
  - Update referensi: `package.json` (termux), `index.js` (install-ffmpeg), `.gitignore` (state/), AGENTS.md.
  - Override env var tetap berlaku; `npm test` (smoke) sandbox via env var tak terpengaruh.
  - Verifikasi: `node test/smoke.js` lulus.

- **03-09-2026** — **FIX Blackjack tidak deal kartu saat semua pemain accept sekaligus**:
  - `handlers/game/casino/commands.js` — Root cause: ketika semua pemain accept sekaligus, kode memanggil `showStatus` langsung tanpa `startGame` → kartu tidak pernah di-deal (tangan kosong, skor 0).
  - Fix: ganti `showStatus(...)` dengan `startGame(sock, chatId, session, session.creator, msg)` — `startGame` sudah handle deal kartu + tampilkan board + schedule timeout.
  - Verifikasi: `node test/smoke.js` 90/90 lulus.

- **03-09-2026** — **RPG: dropdown item stamina saat stamina habis**:
  - `handlers/game/rpg/commands.js` — Helper baru `getStaminaItemRows()` & `handleStaminaDepleted()` — saat `!rpg tebang` (butuh 10) / `!rpg tambang` (butuh 12) stamina tidak cukup, kini kirim `sendMenu` box `⚡ STAMINA HABIS`. Punya item stamina (Apel 🍎 +30 / Roti 🍞 +60) → dropdown `⚡ Isi Stamina` berisi row `!rpg pakai <alias> 1` (pakai 1 langsung, reuse handler `!rpg pakai`). Tidak punya item → tombol `🛒 Buka Toko` (`!rpg shop`). Fallback `BUTTON_MODE=off` → teks biasa.
  - Verifikasi: `node test/smoke.js` 91/91 lulus + test fungsional mock tebang/tambang habis (dengan/tanpa item, BUTTON_MODE off, pakai apel 1 → stamina +30 → tebang sukses).

- **03-09-2026** — **RPG: dropdown `!rpg tempa` 2 level — menu → kategori → craft**:
  - `handlers/game/rpg/commands.js` — `handleRpgCraft`: menu utama ganti dari 79-row 4-section (Senjata 20/Zirah 19/Kapak 20/Beliung 20) → **4-row dropdown kategori** (Pedang/Zirah/Kapak/Pickaxe). Pilih kategori → **dropdown item kategori itu** (bukan teks list) dengan tiap row `id: '!rpg tempa <alias>'` → langsung craft. `costStr`/`mkItemRow` dipindah ke local utility di awal `handleRpgCraft`.
  - **Fix bug filter kapak**: `categoryMap` sebelumnya pakai `t.id.includes('axe')` → ikut match `pickaxe_*` (karena "pickaxe" mengandung "axe") → kapak tampil 40 row. Diganti `t.id.startsWith('axe')` → 20 kapak, 20 pickaxe benar.
  - Fallback `BUTTON_MODE=off`: kategori → teks daftar + instruksi ketik; item → daftar item + instruksi `!rpg tempa [nama]`.
  - Verifikasi: `node test/smoke.js` 91/91 lulus + test fungsional mock (menu 4 kategori, pedang 20 row, zirah 19, kapak 20, pickaxe 20, craft sukses, bahan kurang error).

- **06-09-2026** — **Fitur baru `!playlirik [judul]` — lagu + lirik + card player + audio**:
  - `handlers/media/commands.js` — `handlePlayLirik`: cari di YouTube (`yt-search`), ambil lirik dari **Genius** (`genius-lyrics`, tanpa API key), unduh cover art (YouTube thumbnail HQ), render **card player bergaya aplikasi** (HTML→screenshot via puppeteer), lalu unduh & kirim **file audio** (reuse `runQueuedDownload`).
  - `handlers/media/playerRenderer.js` (baru) — render template HTML → PNG via puppeteer (Chromium headless). Browser di-cache (launch sekali), auto-close saat idle 60s. Punya self-check `node handlers/media/playerRenderer.js`. Gagal render → return `null`.
  - `handlers/media/templates/player.html` (baru) — template card player: gradient gelap, cover art, judul/artis/durasi, tombol kontrol (🔀⏮▶⏭🔁), progress bar + times, blok lirik (max 14 baris).
  - `handlers/router.js` — daftarkan `handlePlayLirik` + token `!playlirik`.
  - `package.json` — tambah deps `genius-lyrics` & `puppeteer`; `allowScripts.puppeteer=true`.
  - **Fallback**: render gagal → card teks plain (info lagu + lirik teks); lirik tak ditemukan → note "Lirik tidak tersedia"; cooldown download 30s (sama `!play`).
  - **Termux**: perlukan `pkg install chromium` + set `PUPPETEER_EXECUTABLE_PATH` di `.env`.
  - **Fix**: Puppeteer v20+ return `Uint8Array` dari `page.screenshot()` (bukan `Buffer`) → Baileys `sendMessage` menolak ("Invalid input type: object"). Dikonversi eksplisit via `Buffer.from(...)` di `playerRenderer.js`.
  - Verifikasi: `node --check` ketiga file lulus, self-check renderer hasil PNG valid, `node test/smoke.js` 91/91 lulus.

- **07-09-2026** — **Fix `!bot` AI: 2 test gagal + anti-crash jalur presence/error**:
  - `handlers/ai/commands.js` — **root cause 2 test smoke gagal** (`sock.sendPresenceUpdate is not a function`): indikator *typing* dipanggil telanjang — di socket nyata bisa throw saat reconnect, di mock test method-nya tidak ada. Kini semua `sendPresenceUpdate` lewat helper `sendPresenceSafe` (try/catch, kosmetik saja — kegagalan tidak mengganggu request AI maupun balasan). Sampah kode `izinMatch` (variabel tak terpakai) dihapus.
  - `handlers/ai/commands.js` — **catch block anti-crash**: bila `sendMessage` balasan error ikut throw (socket mati / transport hilang), error baru tidak lagi menimpa error asli — di-log terpisah. Sebelumnya bisa melempar error kedua yang mengalahkan penyebab pertama.
  - `handlers/ai/memory.js` — `ai_memory.json` kini ditulis **atomik** (file `.tmp` lalu rename, pola sama dengan `data/db.js`) — anti file setengah jadi bila proses mati di tengah penulisan (fakta ingatan owner korup/tumpang-tindih).
  - Verifikasi: `node --check` 2 file lulus + `node test/smoke.js` **91/91 lulus** (sebelumnya 89 lulus, 2 gagal).

- **07-09-2026** — Versi 1.15: penambahan **Sistem Bank & Begal (Rob)**:
  - Baru: `handlers/economy/bank.js` — pusat perbankan dan aksi kriminal antar-pemain.
  - Perintah Bank: `!bank`, `!depo <jumlah|all>` / `!deposit`, `!tarik <jumlah|all>` / `!withdraw` / `!wd`, `!klaimbunga`.
  - Bunga Bank: 1% per 24 jam dengan cap keamanan ekonomi harian.
  - Perlindungan Saldo: Saldo di dalam bank 100% aman dan kebal dari aksi begal (`!begal`).
  - Perintah Begal: `!begal @user` / `!rob @user` — peluang sukses 50% untuk merampas 10-30% saldo cash lawan. Jika gagal, pelaku membayar denda ke korban + cooldown 20 menit. Korban dengan cash < 500 tidak dapat dibegal.
  - Integrasi UI: Saldo Bank kini ditampilkan di profil (`!me`) dan command terdaftar di `!menu`.
  - DB & Testing: Tambah koleksi `bankData` & `robStats` di `data/db.js`, 6 checks baru di `test/smoke.js` (total 96 test lulus).
- **07-09-2026** — Versi 1.16: penambahan minigame **Buckshot Roulette** (`!buckshot` / `!br`):
  - Baru: `handlers/game/buckshot/` (`state.js`, `bot.js`, `render.js`, `router.js`).
  - Mode Solo vs Dealer Bot: `!br bot [taruhan]` atau `!br [taruhan]` tanpa tag pemain. Bot memiliki kecerdasan buatan dalam penggunaan item, estimasi probabilitas peluru, serta keputusan menembak diri sendiri / lawan.
  - Mode PvP Multiplayer: `!br @user1 [@user2] [@user3] [taruhan]` mendukung 2 sampai 4 pemain dengan sistem whitelist undangan (hanya orang yang ditag yang bisa `!terima` / `!tolak`).
  - Taruhan Opsional: mendukung permainan gratis (fun) atau taruhan Money (`getUserMoney`, `deductMoney`, `addMoney`).
  - Mekanik shotgun & item: peluru acak (🔴 tajam & ⚪ kosong), bonus turn jika peluru kosong saat menembak diri sendiri, 5 item (🔍 kaca, 🚬 rokok, 🪚 gergaji, ⛓️ borgol, 🍺 bir).
  - Integrasi: didaftarkan ke `COMMAND_INDEX` & `HANDLERS` di `handlers/router.js`, tombol & menu di `handlers/utility/menuCommands.js`, serta 9 checks pengujian baru di `test/smoke.js` (total 105 checks lulus).
- **07-09-2026** — Versi 1.17: toggle **PM mode** (`!mode on/off`, owner saja) untuk PM orang asing:
  - Baru: `utils/pmMode.js` — state `state/pm_mode.json` (env override `BOT_PM_MODE_PATH`), default `on`.
  - `on` (default): PM orang asing dibalas "hubungi owner" & pesan tidak diproses (perilaku lama).
  - `off`: PM orang asing diabaikan total (tidak dibalas, tidak diproses); hanya owner yang dilayani.
  - Integrasi: gate di blok validasi PM `index.js`, perintah `!mode` di `handlers/owner/ownerCommands.js` + registrasi `handlers/router.js`, 4 check baru di `test/smoke.js`.
- **08-09-2026** — Versi 1.18: fix **mention & JID di Buckshot Roulette**:
  - `handlers/game/buckshot/render.js` — status "✅ Siap" di daftar undangan kini dihitung `isAccepted` secara **alias-aware** (`sameUser`), jadi penerima yang key JID-nya beda domain (LID vs `@s.whatsapp.net`) tetap tampil sudah menerima.
  - `handlers/game/buckshot/router.js` — semua pesan yang memuat teks tag `@nomor` kini menyertakan array `mentions` (giliran bot pakai item, peringatan "bukan giliranmu" di `!item`/`!tembak`, error borgol 2x berturut-turut) agar tag benar-benar ter-highlight di WhatsApp.
  - `handlers/game/buckshot/router.js` — penargetan tembak/borgol kini memakai `resolveTargetInGame` (alias-aware): tag nomor HP yang di-mention dipetakan ke pemain asli dalam game (konsisten dengan key `game.hp`/`game.players`), mencegah damage NaN & tembakan yang tidak mengenai sasaran karena beda domain JID; tambahan validasi target borgol.
  - `test/smoke.js` — 6 checks baru (total 114 lulus).
- **08-09-2026** — Blackjack (`!bj`/`!blackjack`): setiap pemain kini mendapat **2 kartu saat game dimulai**:
  - `handlers/game/casino/commands.js` — `startGame` membagikan 2 kartu per pemain (sebelumnya 1); deteksi **natural blackjack** (skor 21 dari 2 kartu awal) → pemain otomatis ditandai done & diumumkan `🎯 BLACKJACK`, giliran awal dipastikan jatuh ke pemain aktif (bila semua dapat 21 → game langsung resolve); `bjSessions` diekspor untuk keperluan test.
  - `test/smoke.js` — 2 checks baru `testBlackjackDeal` (tantangan dibuat, tiap pemain dapat tepat 2 kartu).
- **08-09-2026** — Fitur kriminal baru **`!bobol`**, **`!top begal`**, & **`!top bobol`**:
  - `!bobol @user` — bobol **tabungan bank** lawan (kebalikan begal: lebih sulit, hasil besar). Cooldown 20 menit, sukses 25% → curi **30–50% saldo bank** korban (min 100); gagal → denda 20–30% cash pembegal. Target butuh minimal 500 saldo bank, pembegal minimal 200 cash. Peringatan di menu `!bank` diperbarui ("aman dari begal, tapi tidak dari bobol!").
  - `!top begal` — leaderboard global pembegal: ranking **total uang curian** (`robStats.stolenTotal`, kini dicatat saat begal sukses) → "💰 Jarahan: Rp X | 🎯 Y sukses / 💀 Z gagal" + mention.
  - `!top bobol` — leaderboard global pembobol (`bobolStats`) → "💰 Jarahan Bank: Rp X | 🎯 Y sukses / 💀 Z gagal" + mention.
  - `data/db.js` — `updateRobStats` kini mencatat `stolenTotal`; baru: `getRobTop`, `getBobolStats`/`updateBobolStats`/`getBobolTop`, `deductBankBalance` (kurangi saldo bank langsung).
  - `handlers/economy/bank.js` — baru: `handleTopBegal`, `handleBobol`, `handleTopBobol`; `robStats` dicatat per-branch (sukses dengan jumlah curian / gagal tanpa).
  - `handlers/router.js` — daftarkan `!bobol`, `!top begal`, `!top bobol`.
  - `test/smoke.js` — 4 checks baru (guard `!bobol` bank kosong ditolak, `!top begal`, `!top bobol`).
- **08-09-2026** — Input angka kini menerima **separator ribuan** (`.`/`,`) dan semua parsing jumlah diseragamkan ke satu util:
  - `utils/amount.js` — `parsePositiveAmount` kini strip separator ribuan sebelum validasi (`10.000`→`10000`, `1,5`→ditolak, `1.5`→ditolak, `10.0000`→ditolak).
  - Migrasi semua handler yang tadinya `parseInt()` manual / `!isNaN` / regex `/^\d+$/` ke `parsePositiveAmount`/`resolveAmount`: `handlers/game/casino/commands.js` (taruhan money, material, ikan, blackjack), `handlers/game/rpg/commands.js` (level monster, beli shop, pakai item, jual material), `handlers/game/fishit/commands.js` (lokasi, jual ikan, beli umpan), `handlers/game/togel/commands.js` (parseBet), `handlers/game/mahjong/commands.js` (parseAnte), `handlers/game/pokemon/commands.js` (toko beli, makan), `handlers/pet/commands.js` (konsumsi ikan), `handlers/nexa/commands.js` (dice, tebak), `handlers/owner/ownerCommands.js` (durasi allowgroup, indeks list).
  - `test/smoke.js` — 11 checks baru parser separator (total 130 lulus).
- **08-09-2026** — **`!ban` berdurasi** — ban bisa diberi waktu otomatis:
  - Sintaks: `!ban @nama 10s` (detik) / `5m` (menit) / `2j` (jam) / `1h` (hari). Tanpa durasi → ban permanen (perilaku lama). Berlaku untuk ban global (owner) & lokal grup (admin).
  - `data/db.js` — `banUser`/`banUserInGroup` menerima param ke-3 `expiresAt`; record berisi `expiresAt` bila ban berdurasi. `isUserBanned`/`isUserBannedInGroup` auto-hapus record yang sudah lewat (lazy expiry, tahan restart bot karena DB dipersist); `getBannedList`/`getGroupBannedList` buang entri expired.
  - `handlers/owner/ownerCommands.js` — helper `parseBanDuration(body)` (regex `(?:^|\s)(\d+)([smjh])\b`), diterapkan ke cabang `!ban` global & lokal (balasan menampilkan label durasi), teks bantuan diperbarui.
  - `test/smoke.js` — 12 checks baru (unit durasi, lazy expiry global & grup, ban permanen, handler `!ban @x 10m`); total 142 lulus.
- **08-09-2026** — **Sistem durasi diseragamkan** (`s/m/h/d/w/mo`) untuk `!ban`, `!allowgroup`, dan terjemahan AI:
  - Skema terpadu: `s` detik · `m` menit · `h` jam · `d` hari · `w` minggu · `mo` bulan (bulan = `mo` karena `m` sudah menit; alias `j` untuk jam dihapus).
  - `utils/duration.js` (baru) — `parseDuration(raw)` jadi satu-satunya parser durasi (regex `(?:^|\s)(\d+)(mo|s|m|h|d|w)\b`).
  - `handlers/owner/ownerCommands.js` — `!ban` dan `!allowgroup` kini memakai `parseDuration`; `!allowgroup` tambah preset `1w` (1 Minggu) & `1mo` (1 Bulan); **bug diperbaiki**: durasi bebas (`1d`, `2h`, `30m`, `1w`, `1mo`, dll) sebelumnya tak pernah lolos guard bantuan, kini benar-benar berfungsi; teks bantuan diperbarui (contoh durasi baru).
  - `handlers/ai/commands.js` — `parseDurasi` selaras: "5 detik"→`5s`, "1 minggu"→`1w`, "1 bulan"→`1mo`, "1 tahun"→`365d`.
  - **Perubahan perilaku**: `!ban @a 2j` kini ditolak → pakai `2h`; `!ban @a 1h` = 1 jam (bukan lagi 1 hari → pakai `1d`).
   - `test/smoke.js` — 12 check baru/update; total 154 lulus.

- **10-09-2026** — Penyegaran tampilan dashboard `!menu` dan `!game`: identitas NEXA PLAYGROUND, command radar, quick-start panel, hierarchy kategori, dan navigasi kembali. Semua command lama tetap dipakai tanpa perubahan perilaku.

- **10-09-2026** — AI owner kini mendukung pencarian internet eksplisit melalui DuckDuckGo Instant Answer: `cari internet ...`, `cari berita ...`, `search ...`, `telusuri ...`, dan `cek terbaru ...`. Hasil web diberikan ke AI beserta URL sumber; pencarian gagal tidak menghentikan AI.

- **10-09-2026** — Persona AI diperbarui agar lebih natural dan emosional: mood adaptif (ceria, manja, cuek, ngambek, tenang), respons fleksibel 1-3 kalimat, serta gaya percakapan tidak lagi selalu seperti template customer service.

- **11-09-2026** — Packname & author stiker diubah menjadi **"yonn kulbet"** untuk semua stiker (biasa & animasi):
  - `handlers/media/commands.js` — `STICKER_META` kini `{ pack: 'yonn kulbet', author: 'yonn kulbet' }`; semua pengiriman stiker (`!s` gambar/video, `!change` mode stiker, `!smeme`) kini meneruskan `pack` & `author` ke konten pesan agar dipakai saat fork Baileys memproses ulang stiker.
  - Perbaikan bug: stiker **animasi** sebelumnya menampilkan "NexusStickers"/"NexusTechPro" (default fork Baileys di `lib/Utils/messages.js:289-290` yang menimpa EXIF saat re-process lewat `compressWebp`); kini ikut memakai "yonn kulbet".
  - `utils/sharp.js` — default `addStickerMeta` diperbarui ke "yonn kulbet".

- **11-09-2026** — Durasi stiker bergerak (`!s` pada video) diperpanjang dari 5 detik menjadi **10 detik** (`-t 10` di konversi ffmpeg `handleSticker`).

- **11-09-2026** — Stiker animasi tidak lagi di-re-encode lewat sharp di fork Baileys (`node_modules/@whiskeysockets/baileys/lib/Utils/messages.js`, cabang `isAnim` di `compressWebp`):
  - Jalur utama (< 1 MB): webp animasi hasil ffmpeg bot dikirim **apa adanya** + tanam EXIF `yonn kulbet` (tanpa `sharp().webp({ effort: 1 })` yang tadi menurunkan kualitas).
  - Jalur fallback (> 1 MB): re-encode ffmpeg `libwebp_anim` sekali saja, kemudian tanam EXIF.
  - ⚠️ Patch ini berada di `node_modules` — **hilang jika `npm install` diulang**.

- **11-09-2026** — Kualitas stiker bergerak diturunkan agar ukuran di bawah 1 MB: `-q:v 50 → 32` di konversi ffmpeg `handleSticker` (`handlers/media/commands.js`). fps/resolusi/durasi tetap. Hasil terukur: pola sibuk ±785 KB, motif ±421 KB — tidak lagi kena re-encode oleh fork.

- **11-09-2026** — Stiker bergerak kini ber-**guard ukuran**: konversi mulai di `q:v 32`, jika webp > 950 KB akan di-encode ulang bertahap (`32 → 26 → 20 → 14`), terakhir fallback `fps=8 + q14`. Video biasa cukup 1× encode (kualitas terjaga), video heavy-motion ditekan hingga dijamin < 1 MB sehingga jalur no-re-encode di fork tetap aktif. Helper `webpFromVideo` + `STICKER_Q_LADDER` di `handlers/media/commands.js`.

- **11-09-2026** — FFmpeg kini **root-only**: binary wajib di dalam folder proyek, sejajar `index.js` (`ffmpeg/bin/ffmpeg.exe`). Semua referensi `C:/ffmpeg/bin` & fallback PATH sistem dihapus:
  - `utils/ffmpeg.js` (baru) — resolver tunggal (kandidat: `<root>/ffmpeg/bin/<name>`, `<root>/<name>`, `<root>/ffmpeg/<name>`); dipakai bersama `handlers/media/download.js` & `index.js`.
  - `handlers/media/download.js` — `FFMPEG_PATH`/`FFPROBE_PATH` dari resolver; `getFfmpegParam()` kini selalu mengirim `--ffmpeg-location <root>/ffmpeg/bin` di **semua** platform (binary tidak lagi di PATH, termasuk Termux).
  - `index.js` — `ffmpegAvailable()` hanya cek path di proyek; teks peringatan menunjuk `install-ffmpeg.bat` (Windows) / taruh binary ELF di `ffmpeg/bin/` + `chmod +x` (Termux).
  - `scripts/install-ffmpeg.bat` — memasang ke `<proyek>\ffmpeg\bin`; `scripts/setup-windows.bat` — cek & info lokasi baru (bukan `where ffmpeg`).

- **11-09-2026** — Redesain penuh dashboard menu WhatsApp `!menu` & `!game` (`handlers/utility/menuCommands.js`):
  - Struktur baru: strip ringkasan, blok kategori ringkas, satu aksen `✦`, divider konsisten (26), tanpa baris kosong liar.
  - Copy sentence-case Indonesia (hapus ALL-CAPS & klise "Pusat kendali", "LEVEL UP AKUN", "DUNIA PETUALANGAN", "MAIN CEPAT", "TOOLBOX", "HIBURAN"); brand jadi `✦ NEXA Playground` / `🎮 NEXA Game Arena`; footer state jelas untuk fallback teks.
  - `utils/ui.js` — helper baru `inlineCmd(command, desc, pad=24)`: baris perintah satu baris `❯ \`cmd\` — deskripsi` dengan alignment via `dlen`. Semua helper lama (`cmd`, `box`, `section`, dst.) tidak berubah.
  - Id perintah & tombol `listButton` dipertahankan — fungsi identik; `buildMenuText`/`buildGameText` diekspor untuk verifikasi rendering.

- **11-09-2026** — `!menu` & `!game` disederhanakan minimalis (`handlers/utility/menuCommands.js`):
  - Judul menjadi **"Menu Utama"** & **"Game Utama"** (hapus brand `NEXA Playground` / `NEXA Game Arena` dari judul & footer); dekorasi berlebih (divider, tagline) dihilangkan, deskripsi dipersingkat.
  - Katalog perintah disinkronkan dengan fitur yang benar-benar ada di `COMMAND_INDEX` — tambah fitur yang sebelumnya tidak tercantum di daftar/tombol: `!mahjong`, `!ttt`, `!suit`, `!gantinama`, `!begal`, `!bobol`, `!top begal`/`!top bobol`, `!playlirik`, `!send`, `!change`, `!toaudio`, `!smeme`, `!getpp`, `!truth`, `!dare`, `!b64`.
  - `!game` menambah section baru (Casino & togel, Mini game) dan tombol navigasi kembali ke `!menu`; `!rpg raid` masuk tombol RPG. Perilaku semua command tetap sama.

- **11-09-2026** — Perbaikan `scripts/setup-termux.sh` (setup Termux):
  - Lokasi proyek ditemukan dengan benar (`cd` ke parent dari folder `scripts/`, apapun cara script dijalankan) — sebelumnya `cd "$(dirname "$0")"` masuk ke folder `scripts/` sendiri sehingga `npm install` berjalan di jalur salah.
  - ffmpeg kini **kondisional**: jika `ffmpeg/bin/ffmpeg` sudah berformat ELF (Termux) → instalasi dilewati; jika berupa `.exe` Windows (`MZ`, bundel `install-ffmpeg.bat`) atau kosong → `pkg install ffmpeg` lalu salin `ffmpeg` + `ffprobe` ke `ffmpeg/bin/` + `chmod +x`. Ini perlu karena `utils/ffmpeg.js` root-only (binari di `$PREFIX/bin` tidak terdeteksi); keberadaan `ffprobe` juga dijamin.
  - `npm install` kini memakai `PUPPETEER_SKIP_DOWNLOAD=1` agar tidak mengunduh Chromium ~170 MB (tidak dapat dijalankan di Termux; `!playlirik` tetap berfungsi via mode teks + lirik tanpa kartu PNG).
  - Cek versi Node ≥ 20 (syarat fork Baileys) sebelum install — dihentikan dengan arahan `pkg install -y nodejs-lts` bila lebih lama; `ffmpeg` dipisah dari paket awal agar `pkg install` tidak sia-sia bila binari sudah ada.
  - Referensi `.env.example` (file tersebut tidak ada di repo) diganti pembuatan template `.env` langsung via heredoc (OWNER_JID, BOT_JID, PAIRING_NUMBER, BUTTON_MODE, DEEPSEEK_*).
  - Verifikasi akhir: `node -e` memastikan `resolveFfmpeg()` menunjuk binari yang benar-benar ada di disk; ramp-check disarankan via `node test/smoke.js`.

---
> Setiap perubahan / penambahan fitur baru: tambahkan entri baru di **Riwayat Perubahan** dan update bagian fitur di atas.
